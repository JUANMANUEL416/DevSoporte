# Copie a ngrok.yml en la raíz del proyecto (no subir authtoken a Git).
# Uso: ngrok start devsoporte --config ngrok.yml
# Producción PM2 escucha en 3300 (API + SPA).
version: "2"
tunnels:
  devsoporte:
    addr: 3300
    proto: http
    # Descomente si tiene dominio reservado en ngrok:
    # domain: crested-gently-landowner.ngrok-free.dev
